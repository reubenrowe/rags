package rnsr.rag.grammar;

import java.util.HashMap;

/**
 * Represents a mapping between the context of one configuration and another, for use in cloning
 */
public	class		ContextMapping
		extends		HashMap<Variable, Variable>
{
	/*
	 * Empty class (Naming Convenience)
	 */
}
